# ==============================================================================
# Luma Shield - Premium Node Scraper Workflow
# ==============================================================================
# Runs the Luma Shield Python scraper every 5 hours or manually. It generates the
# cloud-validated premium VPN node matrix, verifies the JSON artifact, prints a
# short artifact summary, and commits the output only when tracked or newly
# generated content changes. The workflow uses concurrency control, Node 24 opt-in,
# minimal repository permissions, and a timeout to reduce unnecessary CI usage.
# ==============================================================================

name: Luma Premium Node Scraper

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

concurrency:
  group: luma-premium-node-scraper
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  scrape-and-update:
    name: Scrape and Update Premium Nodes
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp

      - name: Run scraper
        run: python main.py

      - name: Inspect generated output
        run: |
          echo "Current directory:"
          pwd
          echo "Repository files:"
          ls -lah
          echo "Checking JSON:"
          test -f luma_premium_nodes.json
          echo "JSON size:"
          wc -c luma_premium_nodes.json
          echo "JSON preview:"
          head -n 30 luma_premium_nodes.json

      - name: Validate JSON
        run: |
          python -m json.tool luma_premium_nodes.json > /tmp/luma_premium_nodes.validated.json
          mv /tmp/luma_premium_nodes.validated.json luma_premium_nodes.json

      - name: Commit and push updated nodes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -f luma_premium_nodes.json
          git status --short
          if git diff --cached --quiet; then
            echo "No node changes detected."
            exit 0
          fi
          git commit -m "Auto-update premium VPN nodes"
          git push
